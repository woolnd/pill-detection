# pill-detection

AI Hub 「경구약제 이미지 데이터」로 YOLO 알약 탐지/분류. 4인 팀, 약 4일.

- 입력: 여러 알약이 함께 놓인 이미지
- 출력: 알약별 바운딩 박스 + 클래스 + 신뢰도
- 대상: **조합경구약제 데이터만**, 클래스 10~15개 선정 (선정 기준은 `docs/`에 문서화)

## 데이터

전년도 프로젝트의 정제된 어노테이션(`image_annotations.csv`)을 1순위로 사용한다. 조합경구약제만 필터링되어 있고 이미지 통합·JSON 정제·손상 이미지 제거가 끝난 상태.

- 이미지 2,050장 (4알 1,906 / 3알 144), 박스 8,056개, 클래스 82개
- bbox는 `(x, y, w, h)` 픽셀 좌표 (`w * h == area`로 검증). COCO JSON이 아니라 CSV이므로 **CSV→YOLO 변환**이다.
- 이미지 실물은 별도 공유 드라이브. 못 쓰게 되면 AI Hub(`datasetkey 576`) 폴백 — `aihubshell -mode l`로 조합경구약제 filekey를 골라서 받는다(전체는 수백 GB). API 키는 절대 커밋 금지.

클래스 선정 시 주의: 조합 이미지라 한 장에 3~4알이 있고 고유 조합이 894개다. 상위 15개 클래스만 골라 "4알 전부 포함"인 이미지만 쓰면 165장밖에 안 남는다. 선정 클래스에 해당하는 박스만 라벨로 남기고 나머지 알약은 라벨 없이 두는 방식으로 1,645장을 확보한다. 이 트레이드오프를 `docs/`에 기록한다.

## 환경

`uv`로 통일. Python 3.13. **`pip` 사용 금지.**

```bash
uv sync                # 환경 재현 (팀원은 git pull && uv sync)
uv add <pkg>           # 의존성 추가 → uv.lock 반드시 커밋
uv run python <file>   # 실행
```

프로젝트 초기 상태라 의존성은 아직 비어 있음. 요청 없이 임의로 패키지를 추가하지 않는다.

## 디렉터리

```
data/raw/    # AI Hub 원본 (gitignore)
data/yolo/   # 변환된 YOLO 데이터셋 (gitignore)
src/         # 전처리·학습·평가 스크립트
docs/        # 클래스 선정 기준, 분석 문서
```

데이터는 절대 커밋하지 않는다. 경로는 하드코딩 대신 인자/상수로.

## 파이프라인 단계

1. 데이터 준비 — 어노테이션 CSV·이미지 확보, 클래스 선정
2. 전처리 — 클래스 필터링 → CSV→YOLO 변환 → train/val 분할
3. 학습 — YOLO 베이스라인 → 하이퍼파라미터 조정 → (여유 시) 비교 모델
4. 평가 — mAP, 클래스별 Precision/Recall, 시각화, confusion matrix, 실패 사례
5. 문서화 — README + 재현성 검증

## 컨벤션

타입은 `feat` / `fix` / `chore` / `design` / `docs` / `refactor` 6개.

| 대상 | 형식 | 예시 |
|---|---|---|
| Branch | `타입/#이슈번호-작업내용` | `feat/#11-csv-to-yolo` |
| Commit | `타입: 작업 내용` | `feat: CSV→YOLO 라벨 변환 구현` |
| Issue / PR | `[타입] 작업 내용` | `[Feat] CSV→YOLO 라벨 변환` |

- 브랜치/커밋 타입은 소문자, Issue/PR 타입은 대괄호 + 첫 글자 대문자.
- 작업 내용은 한글로 간결하게.

## 작업 규칙

- 단계별 브랜치 1개에 4명이 함께 작업. 시작 전 `git pull`, 완료 즉시 `git push`.
- 커밋/푸시는 요청받았을 때만. 남이 작업 중인 파일을 임의로 리팩터링하지 않는다.
- 재현 가능하게: 같은 입력 → 같은 출력. 랜덤 시드 고정.
- 요청한 범위만 작업한다. 스캐폴딩·추상화 미리 만들지 않는다.
- 결과 수치(mAP 등)를 추정해서 쓰지 않는다. 실제 실행 로그의 값만 기록한다.
